# --- Core runtime ---
PySide6>=6.5.0
watchdog>=2.1.6
paramiko>=3.4.0
certifi>=2023.7.22
keyring>=24.0.0

# --- Data handling & analysis (optional but used in RAC tools) ---
pandas>=1.5.0
numpy>=1.23.0
scikit-learn>=1.2.0
nltk>=3.8.1

# --- Visualization / optional analytics ---
matplotlib>=3.6.0
seaborn>=0.12.0

# --- Build & packaging ---
pyinstaller>=6.0.0

# --- Testing ---
pytest>=7.0.0

# --- Comments / meta ---
# Wenn du die App mit PyInstaller baust:
#    ./.venv/bin/pyinstaller PRisM-RAC_arm64.spec
# Wenn du alle Dependencies installieren willst:
#    pip install -U -r requirements.txt
# Optional: falls du macOS-Keychain-Zugriff brauchst, bleib bei keyring>=24
# Hinweis: ftplib, smtplib, ssl, json etc. sind Teil der Standardbibliothek